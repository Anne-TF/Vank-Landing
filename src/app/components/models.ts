export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Country {
  name_en: string;
  name_es: string;
  dial_code: string;
  code: string;
}